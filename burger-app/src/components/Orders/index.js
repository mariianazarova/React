import styled from "styled-components";

const Orders = () => {
  return <OrderStyled> Orders </OrderStyled>;
};

const OrderStyled = styled.div({
  backgroundImage: "grey",
  color:"white"
});

export default Orders;
